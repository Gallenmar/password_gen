services:
  app:
    build:
      context: ..
      dockerfile: build/package/Dockerfile.dev
    volumes:
      - ..:/app
      - ../data:/data
      - go-mod-cache:/go/pkg/mod
    working_dir: /app

volumes:
  go-mod-cache: